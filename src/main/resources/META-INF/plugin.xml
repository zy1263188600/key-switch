<idea-plugin>
    <id>com.github.zy1263188600.keyswitch</id>
    <name>key-switch</name>
    <vendor>zy1263188600</vendor>

<!--    <depends>com.intellij.java</depends>-->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!--        设置-工具中 配置页面-->
        <postStartupActivity implementation="editoraction.PluginStartup"/>

        <notificationGroup id="key-switch" displayType="BALLOON" isLogByDefault="true"/>
        <postStartupActivity implementation="PluginStartupListener"/>
        <!-- 应用级配置 -->
        <applicationService serviceImplementation="state.SettingsState"/>
        <!-- 注册到设置页面 -->
        <applicationConfigurable
                groupId="tools"
                instance="view.PluginSettingsConfigurable"
                displayName="key-switch"/>
    </extensions>
</idea-plugin>
